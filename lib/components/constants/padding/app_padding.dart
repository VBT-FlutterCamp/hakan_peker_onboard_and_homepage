import 'package:flutter/material.dart';

class AppPadding {
  static const EdgeInsets inboxtitlePadding = EdgeInsets.only(left: 15, top: 30, right: 100);
  static const EdgeInsets rowPadding = EdgeInsets.only(right: 8, left: 8);
  static const EdgeInsets pageTopPadding = EdgeInsets.only(top: 15);
}
